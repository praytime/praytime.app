{
  "name": "praytime.app",
  "version": "1.0.0",
  "description": "https://praytime.app frontend",
  "private": true,
  "scripts": {
    "build": "rm -rf ./dist/* && webpack --mode=development",
    "preserve": "rm -rf ./dist/* && webpack --mode=development",
    "serve": "firebase serve --only hosting",
    "predeploy": "rm -rf ./dist/* && webpack --mode=production",
    "deploy": "firebase deploy --only hosting",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/praytime/praytime.app.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/praytime/praytime.app/issues"
  },
  "homepage": "https://github.com/praytime/praytime.app#readme",
  "devDependencies": {
    "copy-webpack-plugin": "^10.2.4",
    "html-webpack-plugin": "^5.5.0",
    "webpack": "^5.71.0",
    "webpack-cli": "^4.9.2"
  },
  "dependencies": {
    "@googlemaps/js-api-loader": "github:googlemaps/js-api-loader",
    "geofire-common": "^5.2.0",
    "suncalc": "^1.9.0"
  }
}
